spring:
  application:
    name: events-service

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost:3306}/arqui?useSSL=true&allowPublicKeyRetrieval=true
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:root}

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    defer-datasource-initialization: true

  sql:
    init:
      mode: always

  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER

  security:
    user:
      password: root

springdoc:
  enable-native-support: true
  api-docs:
    enabled: true
    path: /events-service/v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    url: /events-service/v3/api-docs
    config-url: /events-service/v3/api-docs/swagger-config
    urls:
      - url: /events-service/v3/api-docs
        name: events-service
        display-name: events-service

server:
  port: 0
  servlet:
    context-path: /

eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_CLIENT:localhost:8761}/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}

logging:
  level:
    root: DEBUG

management:
  endpoints:
    web:
      exposure:
        include: mappings

openapi:
  service:
    title: events-service doc
    version: 1.0.0
    url: http://${URL_SERVICE:localhost}:8080
